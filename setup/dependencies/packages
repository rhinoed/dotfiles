# ML4W OS - Dotfiles for Hyprland on FreeBSD 14.x / 15.x
# FreeBSD pkg package names. Lines starting with # are ignored.
#
# NOTE: quickshell is available from the standard repo (0.3.0_1, confirmed).
# awww is available on the 'latest' repo branch only (not quarterly).
# If pkg reports it as missing, switch to latest:
#   mkdir -p /usr/local/etc/pkg/repos
#   echo 'FreeBSD: { url: "pkg+http://pkg.FreeBSD.org/${ABI}/latest" }' > /usr/local/etc/pkg/repos/FreeBSD.conf

# --- Core utilities ---
wget
curl
git
rsync
unzip
jq
vim
bash
sudo
gmake
ccache
ca_root_nss
fswatch
figlet
gum
fastfetch
htop
btop
fzf
eza
zsh
python3
# py-pip removed; use python3 -m ensurepip
pipx
# py-gobject3 removed
# py-screeninfo removed
xclip
rust

# --- System / session ---
# elogind is a core dependency; if it fails, try 'sudo pkg update -f'
# elogind removed
dbus
polkit
hyprpolkitagent
sddm
seatd
drm-kmod
# NOTE: NVIDIA users, install graphics/nvidia-driver and add the module
# to kld_list (see setup/preflight-freebsd.sh).

# --- Networking (FreeBSD-native: wpa_supplicant + ifconfig + dhclient) ---
wifimgr
# Blueman is optional and requires the FreeBSD Bluetooth stack setup.
# blueman

# --- Hyprland ecosystem ---
hyprland
hyprpaper
hyprlock
hypridle
hyprpicker
hyprsunset
hyprshutdown
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
xwayland
waybar
rofi
swaync
nwg-look
# nwg-displays is Linux-only
quickshell
awww
hyprland-guiutils

# --- Wayland tools ---
grim
slurp
wl-clipboard
cliphist
pipewire
wireplumber
playerctl
# imagemagick removed
tesseract
# NOTE: Tesseract language data can be fetched with `pkg search tesseract`.

# --- Desktop apps ---
kitty
vlc
pavucontrol
nautilus
# tumbler removed: not found in current repo branch
gnome-themes-extra
kf6-breeze-icons-6.28.0
qt6ct
qt6-wayland
firefox

# --- Storage ---
# udisks2
# py-udiskie

# --- Fonts ---
font-awesome
noto-emoji
nerd-fonts-jetbrainsmono
nerd-fonts-firacode
# Noto fonts for emoji fallback:
# noto-basic